<%= form_with(model: review, local: true) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :story_id %>
    <%= form.text_field :story_id, id: :review_story_id %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.number_field :rate, id: :review_rate %>
  </div>

  <div class="field">
    <%= form.label :autor %>
    <%= form.text_field :autor, id: :review_autor %>
  </div>

  <div class="field">
    <%= form.label :donation %>
    <%= form.text_field :donation, id: :review_donation %>
  </div>

  <div class="field">
    <%= form.label :market_value %>
    <%= form.text_field :market_value, id: :review_market_value %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :review_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
