<%= form_with(model: story, local: true) do |form| %>
  <% if story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :account_id %>
    <%= form.text_field :account_id, id: :story_account_id %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id, id: :story_category_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :story_title %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :story_active %>
  </div>

  <div class="field">
    <%= form.label :publish_on %>
    <%= form.datetime_select :publish_on, id: :story_publish_on %>
  </div>

  <div class="field">
    <%= form.label :likes %>
    <%= form.number_field :likes, id: :story_likes %>
  </div>

  <div class="field">
    <%= form.label :dislikes %>
    <%= form.number_field :dislikes, id: :story_dislikes %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :story_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
